# AoC 2023
To run these python codes,
- **if you are on VS code:**
  - toggle `python.terminal.executeInFileDir` to `on` and run the code.
- **otherwise:**
  - open `dayX` folder to execute code.